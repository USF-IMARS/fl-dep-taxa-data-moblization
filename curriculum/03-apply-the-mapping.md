Now that you have created a mapping, you can apply that mapping to create an occurrence.csv.
We recommend using something similar to the following AI prompts:

```
Use the following mapping to create a Darwin Core Occurrence table.
Save the table as `occurrence.csv`.
```

```
Write a {programming language} script to create a Darwin Core Occurrence table from the following column mapping.
Save the table as `occurrence.csv`.
```

Once you have an `occurrence.csv` we will move on to checking each column.
